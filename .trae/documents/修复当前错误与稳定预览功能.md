## 不与其他执行冲突的保障
- 不重启或终止现有开发服务：保持 `npm run dev` 在终端 5 运行，不进行任何会中断它的操作。
- 仅进行小范围、可回滚的代码改动：修改局部组件与工具函数，不改动路由、全局配置与 Store 结构。
- 延后依赖变更：`@types/react-router-dom` 的清理作为独立步骤，需你确认后在空闲窗口执行，避免影响其他任务。
- 验证使用现有服务：验证通过页面行为完成，不新增或切换终端；若需命令行验证，使用空闲终端 6，避免影响他人。

## 计划修复项（精确位置）
- 修复文件预览点击错误：`src/components/FileList.tsx:66-70`，去除变量遮蔽，自引用导致的 ReferenceError，改为直接 `setPreviewFile(file)`。
- 修正 PDF.js 入参：
  - 文档信息：`src/lib/pdfService.ts:22-26` 将 `pdfjsLib.getDocument(file)` 改为：当入参为 `File` 时使用 `{ data: await file.arrayBuffer() }`；当为字符串（含 `blob:`/`http(s)`）时直接传字符串或 `{ url }`。
  - 预览调用：`src/components/PDFPreview.tsx:36-41` 和 `src/components/PDFPreview.tsx:58-66`，按上述接口传入 `arrayBuffer` 或 `blob URL`，以确保页数与缩略图生成不失败。
- Lint 清理（非功能性）：移除未使用导入，例如 `PDFPreview.tsx:1-2` 的 `Download` 和 `FileUpload.tsx:2` 的 `File` 图标。

## 执行步骤
- 步骤 1：修复 `FileList` 预览点击，验证能打开预览对话框。
- 步骤 2：修正 `PDFService` 与 `PDFPreview` 的调用契约，验证能正确读到页数与生成缩略图。
- 步骤 3：清理未使用导入并运行 Lint（在空闲窗口进行），确保无质量警告。
- 步骤 4（可选，需你确认）：在不影响他人的时间窗口移除 `@types/react-router-dom` 并重新安装依赖。

## 验证与回滚
- 验证：在“上传”和“文件列表”页面上传实际 PDF，点击眼睛图标打开预览，切换页、缩放、旋转均无错误提示。
- 回滚：若发现与其他任务产生影响，按文件级别回滚最近一次补丁（改动均保持原子化，易于恢复）。

如你确认无冲突担忧，我将按以上步骤实施，每一步修改后立即验证并报告结果。